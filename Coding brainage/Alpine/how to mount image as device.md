Предположим у нас имеется зашифрованная файловая система - это какой-то файл с разметкой для fs внутри и с данными, но это всё надо сначала расшифровать.

```bash
cat LUKS_passphrase | cryptsetup luksOpen encrypted-image.img /decrypted
```

в папку decrypted будет свалена штука расшифрованная

Теперь надо смонтировать файловую систему, чтобы заглянуть внутрь средствами линукса, при этом нужно использовать loop-back девайс, нельзя просто взять и смонтировать папку

```bash

```