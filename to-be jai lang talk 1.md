exceptions are bad because the stacking of functions is compilcated and bloated on the language-level

Memory blocked heap-allocated memory of structs - as a decorator of a field. Orthogonal feature??

native \[de\]serialization

program specifies how to build it (no external, system-specific tools required, lol how)

preprocessor: decorators(?) that tell the compiler to call some code at compile-time